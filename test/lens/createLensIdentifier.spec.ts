// import {createLensIdentifier} from '../../src/type-node-tree'

// test('create getXFromY', () => {
//   expect(createLensIdentifier('Y', 'X')).toBe('getXFromY')
// })

// test('should convert origin type to title case', () => {
//   expect(createLensIdentifier('origin', 'Prop')).toBe('getPropFromOrigin')
//   expect(createLensIdentifier('cat', 'Prop')).toBe('getPropFromCat')
// })

// test('should convert prop name to title case', () => {
//   expect(createLensIdentifier('Origin', 'prop')).toBe('getPropFromOrigin')
//   expect(createLensIdentifier('Origin', 'cat')).toBe('getCatFromOrigin')
// })

// test('should convert camelCase prop names to title case', () => {
//   expect(createLensIdentifier('Origin', 'camelCase')).toBe('getCamelCaseFromOrigin')
// })

// test('should convert hyphen-separated prop names to title case', () => {
//   expect(createLensIdentifier('Origin', 'hyphen-separated')).toBe('getHyphenSeparatedFromOrigin')
// })

test('test', () => {
    expect(true).toBe(true)
})